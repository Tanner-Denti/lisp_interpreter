#ifndef PSI_TYPES_H
#define PSI_TYPES_H

typedef enum {
    NUMBER,
    BOOLEAN,
    STRING,
    SYMBOL,
    LIST,
    CELL,
    ERROR,
    FUNCTION,
} PVAL_TYPE;

typedef enum { BUILTIN, LAMBDA } FUNCTION_TYPE;

typedef enum {
    DIVISION_BY_ZERO,
    INCOMPLETE_PARSE,
    INVALID_TOKEN,
    VALUE_ERROR,
    TYPE_ERROR,
    UNBOUND,
    ARITY,
    INAPPLICABLE_HEAD,
    PROTECTED_SYMBOL,
    ARGLIST,
} ERROR_TYPE;

#endif
